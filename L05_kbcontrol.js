function setup() {
    createCanvas(200,200);
    background(220);
}

function draw() {
    background(220);

    circle(100, 100, 50);
}

function keyPressed() {
    if (key == "w") {
        
    }
}