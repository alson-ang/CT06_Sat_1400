function setup(){
    createCanvas(600, 400);
    background(220);
}

function draw(){
    fill(255, 0, 0);            // Colour inside shape (r, g, b)
    ellipse(300,200,200,200);   // (width, height, x, y) 
    
    // Todo: Challenge 3 Traffic Light
}